csms:
  base_url: "ws://localhost:8180/steve/websocket/CentralSystemService/"
  security:
    profile: plain
  call_timeout_secs: 30

store:
  path: "./data/store"

mqtt:
  host: "localhost"
  port: 1883
  topic_prefix: "batteries"

grpc:
  listen: "0.0.0.0:50051"

charge_points:
  - id: "CP-0001"
    vendor: "GolainEV"
    model: "Edge-1"
    device:
      kind: mqtt
      battery_id: "battery-1"